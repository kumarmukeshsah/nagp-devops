pipeline{
    agent any
    environment{
        notifyEmail = "mukesh.sah@nagarro.com"
    }
    tools{
        maven 'MAVEN_HOME'
    }
    triggers{
        cron('0 06 * * *')
    }
    stages{
        stage("code checkout"){
            steps{
                bat "echo new change"
            }
        }
        stage("code build"){
            steps{
                bat "mvn clean"
            }
        }
        stage("code test"){
            steps{
                 bat "mvn test"
            }
        }
        stage("Publish to Artifactory"){
            steps{
                rtMavenDeployer(
                    id: 'deployer',
                    serverId: 'nagp_test',
                    releaseRepo: 'nagpLocalRepo',
                    snapshotRepo: 'nagpLocalRepo'
                )
                rtMavenRun(
                    pom: 'pom.xml',
                    goals: 'clean install',
                    deployerId:'deployer'
                )
                rtPublishBuildInfo(
                    serverId: 'nagp_test'
                )
            }
        }
    post{
        success{
            bat "echo success"
        }
    }
}
